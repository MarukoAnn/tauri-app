import React from "react";

const styles = {
    width: '100vw',
    height: '6vh',
    backgroundColor: '#fff',
    position: 'fixed',
    top: 0,
    left: 0
}

const WinBar: React.FC = () => {
    return (
        <div style={styles}>顶部</div>
    )
}

export default WinBar;